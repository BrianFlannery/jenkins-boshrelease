<OBXml>
<branches field="source" >
<branch value="ourScratch1" >
</branch>
<branch value="CloudBees" >
#!/usr/bin/env bash

# job template binding variables

# job name &amp; index of this VM within cluster
# e.g. JOB_NAME=redis, JOB_INDEX=0
export NAME='&lt;%= name %>'
export JOB_INDEX=&lt;%= index %>
# full job name, like redis/0 or webapp/3
export JOB_FULL="$NAME/$JOB_INDEX"

export PORT=&lt;%= p("jenkins.server.internal_http_port") %>

export JVM_ARGS='&lt;%= p("jenkins.jvm_args") %>'

</branch>
<branch value="cloudfoundry-community" >
#!/usr/bin/env bash

# job template binding variables

# job name &amp; index of this VM within cluster
# e.g. JOB_NAME=redis, JOB_INDEX=0
export NAME='&lt;%= name %>'
export JOB_INDEX=&lt;%= index %>
# full job name, like redis/0 or webapp/3
export JOB_FULL="$NAME/$JOB_INDEX"

export PORT=&lt;%= p("jenkins.server.internal_http_port") %>
export AJP13_PORT=&lt;%= p("jenkins.server.ajp13_port") %>

export ADMIN_USER=vcap
export ADMIN_PASSWORD='&lt;%= p("jenkins.admin.password") %>'
</branch>
</branches>
</OBXml>
